import type { AppProps } from "next/app";

export default function home({ Component, pageProps }: AppProps){
    return(
        <div>
            <main>
                hello
            </main>
        </div>
    );
}
